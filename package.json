{
  "name": "exam-evaluator-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:mobile": "pnpm --filter mobile dev",
    "dev:api": "pnpm --filter api dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
